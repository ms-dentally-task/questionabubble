<!DOCTYPE html>
<html>
  <head>
    <title>Questionabubble</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <a href="#content" class="sr-only focus:not-sr-only">
      Skip to content
    </a>
    <%= render 'shared/logo_with_text' %>
    <main class="container mx-auto mt-16 px-5" id="content">
      <% if notice %>
        <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
          <%= notice %>
        </p>
      <% end %>
      <% if alert %>
        <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block" id="alert">
          <%= alert %>
        </p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
